<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Edit</title>
</head>
<body>
    <div class="flex justify-center bg-zinc-800 items-center h-screen">
        <div class="w-full max-w-md bg-zinc-900 p-10 text-white rounded-lg">
            <div class="nav text-center mb-4">
                <a class="text-zinc-500 mb-8 bg-zinc-200 px-4 py-1 rounded-lg" href="/read">Read Users</a>
            </div>
            <h1 class="text-center uppercase text-3xl mb-3">Update user</h1>
            <form action="/update/<%= user._id %>" method="post" class="flex flex-col gap-y-4">
                <input class="px-5 py-2 bg-transparent border border-zinc-700 outline-none mb-4" type="text" name="name" placeholder="Enter Your Name" value="<%= user.user %>">
                <input class="px-5 py-2 bg-transparent border border-zinc-700 outline-none mb-4" type="text" name="email" placeholder="Enter Your Email" value="<%= user.email %>">
                <input class="px-5 py-2 bg-transparent border border-zinc-700 outline-none mb-4" type="text" name="image" placeholder="Image URL" value="<%= user.image %>">
                <input class="px-5 py-2 bg-yellow-500 rounded-lg text-black font-semibold border border-zinc-700 outline-none" type="submit" value="Update User">
            </form>
        </div>
    </div>
</body>
</html>
